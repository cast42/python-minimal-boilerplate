# Claude Instructions

Read and follow `../AGENTS.md` for shared repository rules.

When creating a Python project from this template or changing its project setup, use the `minimal-python-boilerplate` skill exposed through `.claude/skills`.
