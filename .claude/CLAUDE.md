# Claude Instructions

Read and follow `../AGENTS.md` for shared repository rules.

When creating a Python project from this template or changing its project setup, use the `minimal-python-boilerplate` skill exposed through `.claude/skills`. If that skill is not discovered (e.g. the symlink was checked out as a plain file on Windows), read and follow `.agents/skills/minimal-python-boilerplate/SKILL.md` directly.
