[project]
name = "python-minimal-boilerplate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "logfire>=4.14.2",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.5.7",
    "mkdocs>=1.6.0",
    "ty>=0.0.1a24",
]
